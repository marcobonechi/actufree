# Dart / Flutter build output
.dart_tool/
.packages
build/

# The workspace resolves to a single root pubspec.lock. It is committed
# deliberately: this workspace ships an app, so builds should be reproducible.
