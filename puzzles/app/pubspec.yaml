name: puzzles_app
description: "The shipped puzzle collection app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  blockblast_engine:
    path: ../packages/blockblast_engine
  puzzle_kit:
    path: ../packages/puzzle_kit
  sudoku_engine:
    path: ../packages/sudoku_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
