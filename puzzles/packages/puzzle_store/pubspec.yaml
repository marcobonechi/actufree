name: puzzle_store
description: "Pure-Dart persistence contract shared by the game engines and the Flutter shell."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.13.1

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
