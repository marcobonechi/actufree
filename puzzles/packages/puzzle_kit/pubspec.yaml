name: puzzle_kit
description: "Shared Flutter layer for the puzzle collection: undo/redo, save/resume, settings, stats and shell UI."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.13.1
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  puzzle_store:
    path: ../puzzle_store
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
